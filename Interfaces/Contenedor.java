import java.util.ArrayList;

public interface Contenedor {
    // Función abstracta no implementada con nombre obtenerEstudiantes
    // El tipo de dato que retornará la función será ArrayList<Estudiante>
    // Tipo de dato que retorna la función + nombre de la función + () + ;
    ArrayList<Estudiante> obtenerEstudiantes();
}